# =============================================================================
# Customer UI Environment Configuration
# =============================================================================

# Backend for Frontend (BFF) API URL
# Uses Dapr service invocation for distributed tracing, retries, and observability
# Dapr HTTP endpoint: http://localhost:3580/v1.0/invoke/web-bff-service/method
# The /v1.0/invoke/{app-id}/method prefix is added by bffClient interceptor
REACT_APP_BFF_URL=http://localhost:3580
REACT_APP_BFF_DAPR_APP_ID=web-bff-service
