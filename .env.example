# =============================================================================
# Customer UI Environment Configuration
# =============================================================================

# Backend for Frontend (BFF) API URL
# The customer UI communicates with the backend through the web-bff service
REACT_APP_BFF_URL=http://localhost:8080
