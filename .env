# =============================================================================
# Customer UI Environment Configuration
# =============================================================================

# Backend for Frontend (BFF) API URL
# The customer UI communicates with the backend through the web-bff service via Dapr
# Dapr sidecar for web-bff runs on port 3580 and routes requests using dapr-app-id header
REACT_APP_BFF_URL=http://localhost:3580
